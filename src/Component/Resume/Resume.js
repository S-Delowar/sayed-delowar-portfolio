import React from 'react';
import './Resume.css';

const Resume = () => {
    return (
        <div className="container resume-page">
            Resume page
        </div>
    );
};

export default Resume;